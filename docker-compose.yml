version: "3.7"
services:
  application:
    image: kip:1.0
    build:
      context: .
      args:
        app_version: 1.0
    environment:
      - KIP_SERVER_PORT=10000
      - KIP_SERVER_HOST=0.0.0.0
    ports:
      - 9000:10000


